import React from "react";
import Form from "./Login/Form";


var Login = React.createClass({

  render: function() {
    return (
      <div class="formFlex">
        <Form />
      </div>
    );
  }
});

export default Login;
